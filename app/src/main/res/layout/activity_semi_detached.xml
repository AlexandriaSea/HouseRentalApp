<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/elseBack"
    android:orientation="vertical"
    tools:context=".SemiDetachedActivity">

    <TextView
        android:id="@+id/textViewSemiDetached"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textAlignment="center"
        android:textStyle="bold"
        android:textSize="20dp"
        android:padding="15dp"
        android:text="Semi-Detached Home Listings" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerViewSemiDetached"
        android:layout_width="match_parent"
        android:layout_height="552dp" />

    <Button
        android:id="@+id/buttonSemiDetached"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginLeft="150dp"
        android:text="Checkout" />

</LinearLayout>